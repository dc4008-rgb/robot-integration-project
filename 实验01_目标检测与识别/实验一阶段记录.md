# 实验一阶段记录：目标检测与识别

更新日期：2026-08-24

## 实验目标

在 Jetson Orin NX 上部署基于 YOLOv8 的实时目标检测节点，识别至少两类物体，
通过 ROS 2 发布检测结果，并保存性能与错误案例作为验收证据。

## 今日完成

- 建立数据采集、数据集处理、远程训练、Jetson 部署和验收测试的代码结构。
- 实测 USB 摄像头在 MJPG 1280x720 模式下可稳定输出 30 FPS。
- 使用 COCO 预训练 `yolov8n.pt` 跑通摄像头、YOLO、ROS 2 和浏览器显示链路。
- `/detections` 使用 `vision_msgs/Detection2DArray`，包含类别、置信度和检测框。
- 浏览器页面可实时显示检测框、类别、置信度和端到端 FPS。
- 完成 93 张有效图片的人工校正标注，共 171 个目标框，类别为
	`cup / mouse / keyboard / bottle`；另有 5 张重复图已排除。
- 测试结束后已从公用 Jetson 删除项目代码、测试快照和对应日志；本地保留最新代码。

## 实测基线

| 项目 | 结果 |
|---|---:|
| 摄像头取帧 | 33.8 ms/帧 |
| YOLOv8n `.pt` 推理 | 32.7 ms/帧 |
| 端到端速度 | 15.0 FPS |
| ROS 2 `/detections` | 14.95 Hz |
| 课程最低速度要求 | 5 FPS |

这组数据只证明部署链路与实时性能可行。当前使用 COCO 预训练权重，不能作为最终
识别正确率验收结果。

## 当前状态

| 任务 | 状态 |
|---|---|
| 摄像头与 Jetson 环境检查 | 已完成并实测 |
| 数据采集脚本 | 已完成 |
| 自动预标注与数据集划分 | 93 张、171 框已标注并通过 Ultralytics 加载检查 |
| YOLO 远程训练 | 代码就绪，等待扩充正式数据集 |
| Jetson ROS 2 检测节点 | 已完成并实测 |
| 浏览器实时画面 | 已完成并实测 |
| 自定义模型正确率验收 | 待扩充数据、训练和验收 |
| 错误案例与最终演示视频 | 待正式模型生成 |

## 下一步

1. 优先补采 `bottle`（当前仅 21 个框），并增加不同背景、光照、距离和遮挡场景；
	当前 93 张多数来自同一桌面的连续拍摄，适合验证训练流程，但不足以支持正式验收。
2. 自动预标注后逐张人工核对，并划分训练集和验证集。
3. 在训练服务器上完成迁移学习，保存 `best.pt` 和训练指标。
4. 在 Jetson 上导出 TensorRT 引擎并复测端到端 FPS。
5. 完成 20 次实物测试，统计 Precision、Recall、F1、mAP 和实际正确率。
6. 提交测试报告、错误案例、ROS 2 话题证据和演示视频。

## 仓库数据策略

模型权重、原始数据集、训练输出和视频体积较大，默认不直接提交到 Git。最终需要
归档时，可使用 GitHub Release、Git LFS 或学校指定的外部存储，并在仓库中记录版本
和下载地址。